<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Require_Department_Name_For_Others</fullName>
    <active>true</active>
    <description>$RecordType.Name = &quot;Others&quot; → only applies when the record type is “Others”.
ISBLANK(TEXT(Department_Name__c)) → checks if Department Name (Text) is empty.</description>
    <errorConditionFormula>AND(
    $RecordType.Name = &quot;Others&quot;,
    ISBLANK(Department_Name__c)
)</errorConditionFormula>
    <errorDisplayField>Department_Name__c</errorDisplayField>
    <errorMessage>Department Name is required for Others record type.</errorMessage>
</ValidationRule>
